node_modules
dist
.env

# Keep generated Prisma/client artifacts out of source control
/src/generated/prisma

# Local uploads are only kept for old records. New uploads use Cloudinary.
uploads/*
!uploads/.gitkeep
